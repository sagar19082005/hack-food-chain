{
  "name": "hackfinal-proxy",
  "version": "1.0.0",
  "description": "Local proxy for Gemini generative API to avoid CORS and hide API key",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
